# Source material

Put the original Overleaf CV source here.

Recommended contents:
- exported `.tex` source
- `.sty` files
- `.cls` files
- exported PDF
- any notes about the import process

This folder is intentionally treated as seed material only.
The long-term source of truth should be structured data in `data/`.
